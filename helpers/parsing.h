#ifndef PARSING_H
#define PARSING_H

#include <QString>
#include <QDateTime>

namespace meow {
namespace helpers {

QDateTime parseDateTime(const QString & dateTime);

} // namespace helpers
} // namespace meow

#endif // PARSING_H
